c3d/1.4.4
========================

<https://sourceforge.net/projects/c3d/>

Used under license:

<GNU General Public License version 2.0 (GPLv2)>

Structure creating script (makeroom_c3d_1.4.4.sh) moved to /sw/apps/c3d/makeroom_1.4.4.sh

LOG
---
    Create the .sif file from .def file :
	```
	Bootstrap: docker
	From: ubuntu:24.04

	%environment
	  export LC_ALL=C
	  export PATH=$PATH:/c3d-1.4.4-Linux-gcc64/bin

	%post
	  export LC_ALL=C
	  export DEBIAN_FRONTEND=noninteractive

	  # Update package list and install dependencies
	  apt update && apt install -y --no-install-recommends \
	      wget \
	      glibc-source \


	  wget https://sourceforge.net/projects/c3d/files/c3d/Nightly/c3d-nightly-Linux-gcc64.tar.gz/download --no-check-certificate
	  tar -xvzf download
	  rm download
	```

%runscript
#!/bin/sh
  if command -v $SINGULARITY_NAME > /dev/null 2> /dev/null; then
    exec $SINGULARITY_NAME "$@"
  else
    echo "# ERROR !!! Command $SINGULARITY_NAME not found in the container"
  fi

    /proj/staff/jayant/install-methods/makeroom.sh "-f" "-c" "apps" "-t" "c3d" "-v" "1.4.4" "-d" "C3D is a command-line tool for converting 3D images between common file formats. The tool also includes a growing list of commands for image manipulation, such as thresholding and resampling." "-L" "GNU General Public License version 2.0 (GPLv2)" "-w" "https://sourceforge.net/projects/c3d/"
    ./makeroom_c3d_1.4.4.sh
    source /scratch/TMPFILE_c3d_1.4.4
    source /sw/apps/c3d/SOURCEME_c3d_1.4.4 && cd $TOOLDIR
    source /sw/apps/c3d/SOURCEME_c3d_1.4.4 && cd $SRCDIR
    
    cp <path to>/c3d.sif $PREFIX
    cd $PREFIX
    mkdir bin & cd bin
    ls -s ../c3d.sif c3d
    
    ./sw/apps/c3d/c3d-1.4.4_post-install.sh 
